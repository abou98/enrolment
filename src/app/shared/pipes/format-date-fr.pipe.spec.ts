import { FormatDateFrPipe } from './format-date-fr.pipe';

describe('FormatDateFrPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateFrPipe();
    expect(pipe).toBeTruthy();
  });
});
