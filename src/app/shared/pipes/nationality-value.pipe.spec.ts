import { NationalityValuePipe } from './nationality-value.pipe';

describe('NationalityValuePipe', () => {
  it('create an instance', () => {
    const pipe = new NationalityValuePipe();
    expect(pipe).toBeTruthy();
  });
});
