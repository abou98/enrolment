import { NationalityLabelPipe } from './nationality-label.pipe';

describe('NationalityLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new NationalityLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
