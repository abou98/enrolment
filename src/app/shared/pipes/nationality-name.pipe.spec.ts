import { NationalityNamePipe } from './nationality-name.pipe';

describe('NationalityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new NationalityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
