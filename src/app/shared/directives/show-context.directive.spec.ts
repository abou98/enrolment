import { ShowContextDirective } from './show-context.directive';

describe('ShowContextDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new ShowContextDirective();
    expect(directive).toBeTruthy();
  });
});
